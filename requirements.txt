# Core
Django~=5.2.0
psycopg[binary]~=3.2
django-environ~=0.12.0

# Server / static
gunicorn~=23.0
whitenoise~=6.8

# Files / storage / images
django-storages~=1.14
boto3~=1.35
Pillow~=11.0

# Frontend interactivity
django-htmx~=1.21

# PDF certificate generation (HTML -> PDF)
# WeasyPrint is the production engine (needs GTK; present in the Docker image).
# xhtml2pdf is a pure-Python fallback for local dev/tests on Windows where GTK
# is unavailable (see MANUAL_SETUP.md); the renderer picks whichever works.
weasyprint~=63.0
xhtml2pdf~=0.2.16

# Excel report export
openpyxl~=3.1

# Dev / test
pytest~=8.3
pytest-django~=4.9
